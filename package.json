{
  "name": "freee-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for freee expense application API on Vercel",
  "scripts": {
    "build": "tsc",
    "dev": "vercel dev",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@slack/web-api": "^7.14.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": "20.x"
  }
}
